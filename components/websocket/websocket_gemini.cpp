/*
 * Repo6 Gemini protocol adapter boundary.
 *
 * This file will contain Gemini-specific framing, JSON/control messages,
 * and conversion between Gemini events and the transport boundary.
 *
 * It must not access I2S, microphone hardware, speaker hardware, or own
 * the Audio Engine's playback/ring buffers.
 */

namespace websocket_gemini {

/* Protocol implementation is intentionally added after the transport
 * structure has been verified. */

} // namespace websocket_gemini
